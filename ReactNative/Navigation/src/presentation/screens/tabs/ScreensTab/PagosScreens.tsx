import { Text, View } from 'react-native'
import { globalStyles } from '../../../theme/theme'

export const PagosScreens = () => {
  return (
    <View style={globalStyles.container}>
        <Text style={{alignSelf:'center'}}>PagosScreens</Text>
    </View>
  )
}
