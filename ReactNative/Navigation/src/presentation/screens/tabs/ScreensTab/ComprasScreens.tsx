import { Text, View } from 'react-native'
import { globalStyles } from '../../../theme/theme'

export const ComprasScreens = () => {
  return (
        <View style={globalStyles.container}>
            <Text style={{alignSelf:'center'}}>ComprasScreens</Text>
        </View>
  )
}
