import { Text, View } from "react-native"
import { globalStyles } from "../../../theme/theme"

export const DescuentosScreens = () => {
  return (
        <View style={globalStyles.container}>
            <Text style={{alignSelf:'center'}}>DescuentosScreens</Text>
        </View>
  )
}
